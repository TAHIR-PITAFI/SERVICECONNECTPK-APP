/**
 * constants/Colors.js — Re-exports from unified Theme
 * Kept for backward compatibility with existing imports
 */
export { Colors, Typography, Spacing, BorderRadius, Shadows } from '../utils/Theme';
